{{ ansible_managed | comment }}
[Unit]
Description=Run apt-mirror regulary
After=network.target
Wants=network-online.target

[Timer]
OnCalendar={{ apt_mirror__systemd_on_calendar }}
RandomizedDelaySec={{ apt_mirror__systemd_random_delay_sec }}
Persistent=true

[Install]
WantedBy=timers.target
